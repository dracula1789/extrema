# lbasdfasdf sadfasdf

def find_maxima(arr):
    """Returns the positions of the maximum values in the array
    arr.

    Example:
    find_maxima([1, 2, 1]) → [1]
    find_maxima([2, 0, 0, -2, 2]) → [0, 4]
    """
    return [0]
